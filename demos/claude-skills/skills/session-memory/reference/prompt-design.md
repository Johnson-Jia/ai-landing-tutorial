# 压缩 prompt 设计原理

## 为什么"用户纠正"权重最高

模型最常犯的错是"回退":用户纠正过的行为,几轮后模型又做回去了。把纠正放最前 + 保留原话,是新会话/续接时最先看到的"红线"。

## 为什么"已完成"可最简

已完成的工作对"接下来做什么"价值最低,压缩时可以最简(一句带过),省 token 给高优先级项。

## 顺序即优先级

章节顺序 = 优先级顺序。LLM 读到靠前的内容权重更高(注意力偏置)。所以排序本身就是设计,不是随意组织。
